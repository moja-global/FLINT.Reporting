form([formgroup]='poolsForm')
  .form-group
    label(for='name') Name
    input#name.form-control(type='text', placeholder='', formcontrolname='name')
    .alert.alert-danger.mt-3(*ngif="poolsForm.get('name').invalid && (poolsForm.get('name').dirty || poolsForm.get('name').touched)")
      div(*ngif="poolsForm.get('name').errors.required")
        | Name is required.
      div(*ngif="poolsForm.get('name').errors.maxlength")
        | Name cannot be more than 50 characters long.
      div(*ngif="poolsForm.get('name').errors.exists")
        | Name should be unique.
  .form-group
    label(for='description')
      | Description 
      span.text-secondary  (Optional)
    textarea#description.form-control(placeholder='', rows='3', formcontrolname='description')
    .alert.alert-danger.mt-3(*ngif="poolsForm.get('description').invalid && (poolsForm.get('description').dirty || poolsForm.get('description').touched)")
      div(*ngif="poolsForm.get('description').errors.maxlength")
        | Description cannot be more than 250 characters long.
