sb-modal
  ng-container(title='')
    | Delete {{targetReportingFramework.name}} Reporting Table Record
  ng-container(body='')
    sb-reporting-tables-records-deletion([targetreportingframework]='targetReportingFramework', [id]='id', (succeeded)='onSucceeded()', (failed)='onFailed($event)')
    ng-container(*ngif='(status$ | async) === "deleting"')
      sb-message-overlay(activity='deleting', status='deleting')
    ng-container(*ngif='(status$ | async) === "succeeded"')
      sb-message-overlay(activity='deleting', status='succeeded')
    ng-container(*ngif='(status$ | async) === "failed"')
      sb-message-overlay(activity='deleting', status='failed')
    ng-container(*ngif='(status$ | async) === "retrying"')
      sb-countdown-overlay(crosshead='Retrying in ', (completed)='onDelete()')
  ng-container(footer='')
    ng-container(*ngif='(status$ | async) === "new"')
      sb-button(action='yes', entity='Reporting Table', (yes)='onDelete()')
      sb-button(action='no', entity='Reporting Table', guarded='false', (no)='onQuit()')
    ng-container(*ngif='(status$ | async) === "deleting"')
      sb-button(action='yes', entity='Reporting Table', disabled='true')
      sb-button(action='no', entity='Reporting Table', disabled='true')
    ng-container(*ngif='(status$ | async) === "succeeded"')
      sb-button(action='yes', entity='Reporting Table', disabled='true')
      sb-button(action='no', entity='Reporting Table', disabled='true')
    ng-container(*ngif='(status$ | async) === "failed"')
      sb-button(action='yes', entity='Reporting Table', (yes)='onRetry()')
      sb-button(action='no', entity='Reporting Table', guarded='false', (no)='onQuit()')
    ng-container(*ngif='(status$ | async) === "retrying"')
      sb-button(action='yes', entity='Reporting Table', disabled='true')
      sb-button(action='no', entity='Reporting Table', disabled='true')
