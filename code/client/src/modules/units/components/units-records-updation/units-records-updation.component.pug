form([formgroup]='unitsForm')
  .form-group
    label(for='name') Name
    input#name.form-control(type='text', placeholder='', formcontrolname='name')
    .alert.alert-danger.mt-3(*ngif="unitsForm.get('name')?.invalid && (unitsForm.get('name')?.dirty || unitsForm.get('name')?.touched)")
      div(*ngif="unitsForm.get('name')?.errors.required")
        | Name is required.
      div(*ngif="unitsForm.get('name')?.errors.maxlength")
        | Name cannot be more than 50 characters long.
      div(*ngif="unitsForm.get('name')?.errors.exists")
        | Name should be unique.
