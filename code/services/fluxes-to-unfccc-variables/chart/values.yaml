# Default values for the Helm Chart.
# This is a YAML-formatted file.

microservice:

  # Ingress
  ingress:
    name: fluxes-to-unfccc-variables
    class: traefik 
    rules:
      host: reporter.miles.co.ke
      http:
        path: /api/v1/fluxes_to_unfccc_variables
        backend:
          service:
            name: fluxes-to-unfccc-variables
            port: 80

  # Service
  service:
    name: fluxes-to-unfccc-variables
    port:
      default: 80
      target: 8080

  # Deployment
  deployment:
    name: fluxes-to-unfccc-variables

  # Application / Application Replicas
  application:
    name: fluxes-to-unfccc-variables
    version: 0.0.1
    image: 
      id: reporter.miles.co.ke:5043/fluxes-to-unfccc-variables
      tag: 0.0.1
      pull:
        policy: Always
        secret: regcred
    environment: 
      postgres:
        host: bitnami-postgres-postgresql
    replicas: 1

 



